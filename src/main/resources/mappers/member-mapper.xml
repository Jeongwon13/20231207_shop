<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hy.shop.member.model.dao.MemberMapper">

    <insert id="signup" parameterType="com.hy.shop.member.model.vo.Member">
        INSERT INTO MEMBER (
            MEMBER_NO, MEMBER_ID, MEMBER_PW, MEMBER_EMAIL, MEMBER_NAME, MEMBER_NICKNAME,
            MEMBER_PHONE, ADDRESS, MEMBER_BIRTH
        ) VALUES (
                     NULL, #{memberId}, #{memberPw}, #{memberEmail}, #{memberName}, #{memberNickname},
                     #{memberPhone}, #{address}, #{memberBirth}
                 )
    </insert>

</mapper>
